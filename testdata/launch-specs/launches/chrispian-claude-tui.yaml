# Reproduces legacy ~/.tether/catalog/launches/chrispian-claude-tui.yaml.
# Boot-profile folded: chrispian.steward.tui.yaml
# Legacy: project=chrispian / agent=general / provider=claude-pty / workspace.mode=hybrid.
spec: tether.launch
name: chrispian-claude-tui
inputs:
  work_dir: /Users/chrispian/dev/chrispian
  runner: claude-pty
  isolation: hybrid
  project: chrispian
  agent: general
  agent_role: meta/steward
  display_name: "chrispian launchpad — Steward (TUI)"
