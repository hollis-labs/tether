# Reproduces legacy ~/.tether/catalog/launches/agent-mux-opencode.yaml.
# Boot-profile folded: agent-mux.engineer.opencode.yaml
# Legacy: project=tether / agent=general / provider=opencode / workspace.mode=hybrid.
spec: tether.launch
name: agent-mux-opencode
inputs:
  work_dir: /Users/chrispian/dev/hollis-labs/apps/tether
  runner: opencode
  isolation: hybrid
  project: tether
  agent: general
  agent_role: domain/backend/worker
  display_name: "Agent Mux — OpenCode"
