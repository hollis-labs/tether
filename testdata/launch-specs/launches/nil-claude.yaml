# Reproduces legacy ~/.tether/catalog/launches/nil-claude.yaml.
# Boot-profile folded: nil.backend.main.yaml
# Legacy: project=nil / agent=general / provider=claude-code / workspace.mode=hybrid.
spec: tether.launch
name: nil-claude
inputs:
  work_dir: /Users/chrispian/dev/hollis-labs/apps/nil
  runner: claude-code
  isolation: hybrid
  project: nil
  agent: general
  agent_role: domain/backend/worker
  display_name: "Nil — Backend"
