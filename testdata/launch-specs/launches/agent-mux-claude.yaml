# Reproduces legacy ~/.tether/catalog/launches/agent-mux-claude.yaml.
# Boot-profile folded: (no boot-profile; uses spec default role)
# Legacy: project=tether / agent=general / provider=claude-code / workspace.mode=hybrid.
spec: tether.launch
name: agent-mux-claude
inputs:
  work_dir: /Users/chrispian/dev/hollis-labs/apps/tether
  runner: claude-code
  isolation: hybrid
  project: tether
  agent: general
  agent_role: domain/backend/worker
  display_name: "Agent Mux — Backend"
