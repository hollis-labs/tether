# Reproduces legacy ~/.tether/catalog/launches/hadron-claude.yaml.
# Boot-profile folded: hadron.backend.main.yaml
# Legacy: project=hadron / agent=general / provider=claude-code / workspace.mode=hybrid.
spec: tether.launch
name: hadron-claude
inputs:
  work_dir: /Users/chrispian/dev/hollis-labs/apps/hadron
  runner: claude-code
  isolation: hybrid
  project: hadron
  agent: general
  agent_role: domain/backend/worker
  display_name: "Hadron — Backend"
