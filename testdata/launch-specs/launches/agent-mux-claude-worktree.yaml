# Reproduces legacy ~/.tether/catalog/launches/agent-mux-claude-worktree.yaml.
# Boot-profile folded: (no boot-profile; uses spec default role)
# Legacy: project=tether / agent=general / provider=claude-code / workspace.mode=worktree.
spec: tether.launch
name: agent-mux-claude-worktree
inputs:
  work_dir: ~/dev/hollis-labs/apps/agent-mux
  runner: claude-code
  isolation: worktree
  project: agent-mux
  agent: general
  agent_role: domain/backend/worker
  display_name: "Agent Mux — Backend (Worktree)"
