# Reproduces legacy ~/.tether/catalog/launches/torque-claude-stream-worktree.yaml.
# Boot-profile folded: (no boot-profile; uses spec default role)
# Legacy: project=torque / agent=general / provider=claude-stream / workspace.mode=worktree.
spec: tether.launch
name: torque-claude-stream-worktree
inputs:
  work_dir: /Users/chrispian/dev/hollis-labs/apps/torque
  runner: claude-stream
  isolation: worktree
  project: torque
  agent: general
  agent_role: domain/backend/worker
  display_name: "Torque — Backend (Stream, Worktree)"
