# Reproduces legacy ~/.tether/catalog/launches/fragments-engine-claude.yaml.
# Boot-profile folded: fragments-engine.backend.main.yaml
# Legacy: project=fragments-engine / agent=general / provider=claude-code / workspace.mode=hybrid.
spec: tether.launch
name: fragments-engine-claude
inputs:
  work_dir: /Users/chrispian/dev/hollis-labs/apps/fragments-engine
  runner: claude-code
  isolation: hybrid
  project: fragments-engine
  agent: general
  agent_role: domain/backend/worker
  display_name: "Fragments Engine — Backend"
