# Reproduces legacy ~/.tether/catalog/launches/launchpad-orchestrator-claude-stream.yaml.
# Boot-profile folded: launchpad.orchestrator.main.yaml
# Legacy: project=launchpad / agent=launchpad-orchestrator / provider=claude-stream / workspace.mode=hybrid.
spec: tether.launch
name: launchpad-orchestrator-claude-stream
inputs:
  work_dir: /Users/chrispian/dev/agent-os/launchpad
  runner: claude-stream
  isolation: hybrid
  project: launchpad
  agent: launchpad-orchestrator
  agent_role: domain/strategic-planner
  display_name: "Launchpad — Orchestrator (Stream)"
