# Reproduces legacy ~/.tether/catalog/launches/sigil-claude-worktree.yaml.
# Boot-profile folded: sigil.backend.main.worktree.yaml
# Legacy: project=sigil / agent=general / provider=claude-code / workspace.mode=worktree.
spec: tether.launch
name: sigil-claude-worktree
inputs:
  work_dir: ~/dev/hollis-labs/apps/sigil
  runner: claude-code
  isolation: worktree
  project: sigil
  agent: general
  agent_role: domain/backend/worker
  display_name: "Sigil — Backend (Worktree)"
