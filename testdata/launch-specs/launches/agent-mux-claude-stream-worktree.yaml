# Reproduces legacy ~/.tether/catalog/launches/agent-mux-claude-stream-worktree.yaml.
# Boot-profile folded: agent-mux.engineer.main.worktree.yaml
# Legacy: project=tether / agent=general / provider=claude-stream / workspace.mode=worktree.
spec: tether.launch
name: agent-mux-claude-stream-worktree
inputs:
  work_dir: ~/dev/hollis-labs/apps/agent-mux
  runner: claude-stream
  isolation: worktree
  project: agent-mux
  agent: general
  agent_role: domain/backend/worker
  display_name: "Agent Mux — Backend (Stream, Worktree)"
