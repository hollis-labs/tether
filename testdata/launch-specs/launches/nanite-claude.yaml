# Reproduces legacy ~/.tether/catalog/launches/nanite-claude.yaml.
# Boot-profile folded: nanite.backend.main.yaml
# Legacy: project=nanite / agent=general / provider=claude-code / workspace.mode=hybrid.
spec: tether.launch
name: nanite-claude
inputs:
  work_dir: ~/dev/hollis-labs/apps/nanite
  runner: claude-code
  isolation: hybrid
  project: nanite
  agent: general
  agent_role: domain/backend/worker
  display_name: "Nanite — Backend"
