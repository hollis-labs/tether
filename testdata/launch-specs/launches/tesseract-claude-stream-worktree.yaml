# Reproduces legacy ~/.tether/catalog/launches/tesseract-claude-stream-worktree.yaml.
# Boot-profile folded: (no boot-profile; uses spec default role)
# Legacy: project=tesseract / agent=general / provider=claude-stream / workspace.mode=worktree.
spec: tether.launch
name: tesseract-claude-stream-worktree
inputs:
  work_dir: ~/dev/hollis-labs/apps/tesseract
  runner: claude-stream
  isolation: worktree
  project: tesseract
  agent: general
  agent_role: domain/backend/worker
  display_name: "Tesseract — Backend (Stream, Worktree)"
