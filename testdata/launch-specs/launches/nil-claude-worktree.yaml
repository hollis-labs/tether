# Reproduces legacy ~/.tether/catalog/launches/nil-claude-worktree.yaml.
# Boot-profile folded: nil.backend.main.worktree.yaml
# Legacy: project=nil / agent=general / provider=claude-code / workspace.mode=worktree.
spec: tether.launch
name: nil-claude-worktree
inputs:
  work_dir: /Users/chrispian/dev/hollis-labs/apps/nil
  runner: claude-code
  isolation: worktree
  project: nil
  agent: general
  agent_role: domain/backend/worker
  display_name: "Nil — Backend (Worktree)"
