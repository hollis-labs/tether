# Reproduces legacy ~/.tether/catalog/launches/hadron-claude-worktree.yaml.
# Boot-profile folded: hadron.backend.main.worktree.yaml
# Legacy: project=hadron / agent=general / provider=claude-code / workspace.mode=worktree.
spec: tether.launch
name: hadron-claude-worktree
inputs:
  work_dir: ~/dev/hollis-labs/apps/hadron
  runner: claude-code
  isolation: worktree
  project: hadron
  agent: general
  agent_role: domain/backend/worker
  display_name: "Hadron — Backend (Worktree)"
