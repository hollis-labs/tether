# Common-setup template: Claude Code, git-worktree isolation.
# Replaces the legacy `-claude-worktree` TWIN file — now just an input
# value, not a second launch file.
runner: claude-code
isolation: worktree
