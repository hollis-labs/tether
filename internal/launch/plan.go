package launch

import "github.com/hollis-labs/tether/internal/launchprofile"

type (
	// Plan is defined in internal/launchprofile.
	Plan = launchprofile.Plan
	// NativeFile is defined in internal/launchprofile.
	NativeFile = launchprofile.NativeFile
	// SharedLaunchState is defined in internal/launchprofile.
	SharedLaunchState = launchprofile.SharedLaunchState
)
