// Package launchprofile defines Tether's launch configuration types:
// launch profiles (how an agent runs), launch contexts (the workspace and
// project environment for a launch), and resolved launch plans.
//
// Matches Torque's internal/launchprofile and Tachyon's internal/launch +
// internal/launchprofile convention as a flat, self-contained package.
package launchprofile
