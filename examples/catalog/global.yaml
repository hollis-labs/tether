version: 0.1.0
catalog:
  roots:
    projects: projects
    agents: agents
    providers: providers
    launches: launches
    boot: boot
  defaults:
    workspace_root: ~/agent-mux/workspaces
    state_db: ~/agent-mux/state/agent-mux.db
    temp_root: ~/agent-mux/tmp
